import React from "react";

const Footer = () => {
  return (
    <div>
      <h1>
        Ready to take your idea to <button>Start Project</button> the next
        level?
      </h1>
    </div>
  );
};

export default Footer;
